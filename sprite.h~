#ifndef SPRITE_H
#define SPRITE_H

#include <SDL2/SDL.h>
#include <string>
#include <map>
using namespace std;

struct Sprite {
    int width;
    int height;
    Uint32* buffer;
    ~Sprite();
    void loadFromFile(string path);
};

map<string, Sprite> loadSpritesFromDir(string path);

#endif
