#ifndef SPRITE_MAP_H
#define SPRITE_MAP_H

#include <string>
#include <vector>

using namespace std;

class SpriteMap{
public:
    bool loadFromPath(const string path);
private:
    vector<int> mapVector;
    int mapWidth;
};

#endif

// Local Variables:
// mode: c++
// End:
